# Shusteri Automation

Система автоматизации генерации экспортных документов (Invoice, Specification, Packing List) для компании Shusteri.

## Возможности

- Генерация трёх документов: Commercial Invoice, Specification, Packing List
- Два режима вывода: три отдельных `.xlsx` или один файл с тремя листами
- Поддержка пресетов клиентов (Shoesberry, Bootstyle и др.)
- Два режима обработки: **загрузка контейнера** (пары) и **отправка грузов** (полупары)
- Автоматическое разделение товаров по категориям длины стельки (≤24 см / >24 см)
- Интеграция с системой **Честный знак** — добавление КМ/КИЗ кодов маркировки
- Добавление КИЗ кодов в **уже готовый файл** (без перегенерации документов)
- **Графический интерфейс** (CustomTkinter) и CLI-режим
- **Десктопное приложение** — standalone `.exe` без установки Python

## Известные проблемы

| # | Проблема | Статус |
|---|----------|--------|
| 1 | Вставка из буфера обмена (Ctrl+V) не работает в текстовых полях GUI на Windows | ⚠️ Не решена — ограничение CustomTkinter/tkinter на Windows |

> **Workaround:** Используйте правую кнопку мыши → Вставить, или вводите текст вручную.

## Использование (готовый .exe)

1. Скопировать `ShusteriAutomation.exe` в любую рабочую папку
2. Запустить — папки `input/`, `output/`, `presets/`, `выгрузка честный знак/` создадутся автоматически
3. Положить пресеты клиентов в `presets/`, входные файлы в `input/`
4. Готовые документы появятся в `output/`

## Установка для разработки

```bash
pip install -r requirements.txt
pip install customtkinter pyinstaller
```

## Запуск в dev-режиме

### GUI (рекомендуется)

```bash
python ui_run.py
```

### CLI

```bash
python run.py
```

## Сборка .exe

```bash
pyinstaller shusteri.spec
# или двойной клик на build.bat
```

Результат: `dist/ShusteriAutomation.exe` (~40 МБ, не требует установки Python)

## Структура проекта

```
shusteri-automation/
├── ui/                        # GUI интерфейс (CustomTkinter)
│   ├── app.py                 # Главное окно, сайдбар, переключение вкладок
│   └── views/
│       ├── generation.py      # Вкладка «Генерация»
│       ├── presets.py         # Вкладка «Пресеты»
│       ├── history.py         # Вкладка «История»
│       ├── log_view.py        # Вкладка «Лог»
│       └── _inject_dialog.py  # Диалог добавления КИЗ в готовый файл
├── src/
│   ├── generators/            # Генераторы документов (invoice, spec, packing list)
│   ├── formatters/            # Форматирование Excel
│   ├── models.py              # Модели данных (Pydantic)
│   ├── parser.py              # Парсер входных файлов (режим контейнер)
│   ├── shipment_parser.py     # Парсер входных файлов (режим отгрузка)
│   ├── processor.py           # Обработка данных (режим контейнер)
│   ├── shipment_processor.py  # Обработка данных (режим отгрузка)
│   └── kiz_injector.py        # Инъекция КИЗ кодов в готовый файл
├── presets/                   # YAML-пресеты клиентов
├── input/                     # Входные Excel файлы
├── output/                    # Готовые документы
├── выгрузка честный знак/     # Файлы выгрузки КМ кодов из ЧЗ
├── config.yaml                # Конфигурация (размеры, колонки)
├── shusteri.spec              # Конфиг сборки PyInstaller
├── build.bat                  # Скрипт сборки .exe (Windows)
├── run.py                     # CLI точка входа
└── ui_run.py                  # GUI точка входа
```

## Требования

- Python 3.10+
- pandas, openpyxl, rich, pydantic, PyYAML
- customtkinter (GUI)
- pyinstaller (только для сборки .exe)

## История изменений

| Версия | Что добавлено |
|--------|--------------|
| v2.2 | Исправлена колонка «Вид упаковки» в Packing List (съезжала на 1 строку вверх) |
| v2.1 | Десктопный .exe (PyInstaller), автосоздание папок, кнопка «Обзор» для КМ файлов |
| v2.0 | GUI интерфейс на CustomTkinter |
| v1.3 | Режим добавления КИЗ в готовый файл (`kiz_injector.py`) |
| v1.2 | Форматирование документов по эталону |
| v1.1 | Интеграция с системой «Честный знак» (КМ коды) |
| v1.0 | Базовая генерация Invoice, Specification, Packing List |
